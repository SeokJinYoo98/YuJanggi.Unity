# YuJanggi 작업 지침

네 저장소(Core, Server, Unity, Protocol)는 각각 루트에 AGENTS.md를 두어 단독 체크아웃에서도 지침을 확인할 수 있게 한다. 공통 정책을 바꾸면 나머지 저장소의 지침도 확인하고, 저장소별 검증 항목은 각 파일에서 관리한다. 각 저장소의 README.md와 기존 코드 구조를 우선 존중한다.

## 작업 브랜치

- 기본 작업 브랜치는 `dev`이다. 수정 전에 현재 브랜치와 `git status --short`를 확인한다.
- 다른 브랜치에 있다면 사용자 변경을 안전하게 보존할 수 있는 경우 `dev`로 전환한 뒤 작업한다. 전환을 위해 변경을 버리거나 임의로 stash하지 않는다.
- 로컬 `dev`가 없으면 원격 `dev`를 추적한다. 원격에도 없으면 현재 기준 커밋을 확인하고 그 커밋에서 `dev`를 만든다. 기존 브랜치를 reset하거나 강제로 덮어쓰지 않는다.
- `main`에서 직접 수정·커밋하지 않는다. 사용자가 별도 브랜치를 명시하면 해당 요청을 따른다.
- 커밋, 원격 push, 버전 태그 생성·게시 여부는 사용자가 요청한 작업 범위를 따른다. `dev` 사용 자체가 배포를 의미하지 않는다.
## 공통 원칙

- 변경 전 `git status --short`로 기존 사용자 변경을 확인하고, 관련 없는 변경은 수정하거나 커밋하지 않는다.
- 기능 변경에는 가능한 가장 가까운 자동화 테스트를 추가하거나 갱신하고, 변경 범위에 맞는 테스트를 실행한다.
- 새 의존성, 설정 값, 네트워크 프로토콜 변경은 필요한 저장소 모두에 미치는 영향을 확인한다.
- 공개 API, 직렬화 모델, 네트워크 메시지 변경은 하위 호환성 및 호출 지점을 함께 검토한다.
- `ToDo.md`, `ToDoList.md` 등 ToDo 문서는 만들거나 유지하지 않는다. 작업 계획과 후속 작업은 이슈, PR 또는 코드 주석에 기록한다.

## 저장소 경계

- **YuJanggi.Core**: Unity와 서버가 공유하는 순수 C# 장기 규칙 엔진이다. `UnityEngine`, UI, 네트워크, 플랫폼 전용 의존성을 추가하지 않는다.
- **YuJanggi.Server**: 서버 권위형 매치, 연결, 영속화와 네트워크 처리를 담당한다. 게임 규칙과 상태 변경은 Core를 통해 수행한다.
- **YuJanggi.Unity**: 입력, 화면, 애니메이션, 오디오와 클라이언트 흐름을 담당한다. 보드 상태를 우회해 직접 변경하지 않고 Core의 검증된 진입점을 사용한다.

- **YuJanggi.Protocol**: 공용 DTO, 메시지 식별자, 직렬화와 패킷 규약을 담당한다. 게임 규칙, TCP 연결 관리, UnityEngine 의존성을 추가하지 않는다.

## 구현 기준

- 이동·포획·턴·종료처럼 게임 상태를 변경하는 로직은 단일 진입점에서 검증한 뒤 적용한다.
- Core를 변경하면 Unity와 Server가 동일한 Core 버전을 사용한다는 전제를 유지한다.
- 온라인 대국의 최종 규칙 검증은 Server에서 수행한다. 로컬 AI는 Unity에 둘 수 있으나, 선택한 수는 Core 규칙으로 검증한다.
- 코드 스타일과 네임스페이스는 해당 저장소의 기존 관례를 따른다. 포맷만을 위한 대규모 변경은 피한다.

## 검증과 커밋

- 변경한 저장소의 아래 검증 항목을 우선 실행한다. 다른 저장소에 영향이 있으면 해당 소비 측도 검증한다.
- 테스트를 실행하지 못한 경우 원인과 미검증 범위를 명시한다.
- 커밋 메시지는 한글로 작성하며 본문에 변경 이유, 핵심 내용과 빌드·테스트 결과를 적는다. 커밋은 하나의 목적만 담고, 관련 파일만 포함한다. 커밋 메시지는 `feat`, `fix`, `refactor`, `docs`, `test` 형식을 사용한다.

## Unity 검증

- 변경 범위에 해당하는 Unity 테스트와 Editor 컴파일을 확인한다. 생성된 csproj 빌드만으로 Player 동작 검증을 대신하지 않는다.
- 온라인 흐름 변경은 서버 연결, 매칭, 대국 진행을 확인한다. 실행할 수 없으면 미검증 범위를 명시한다.
- Core·Protocol 버전 변경 시 서버와의 정렬, 중복 DLL, JSON 런타임 의존성을 확인한다.
